{
  "mcpServers": {
    "empathy": {
      "command": "python",
      "args": ["-m", "empathy_os.mcp.server"],
      "env": {
        "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}",
        "PYTHONPATH": "${workspaceFolder}/src"
      },
      "disabled": false
    }
  }
}
