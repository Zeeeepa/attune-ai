# Empathy Framework Environment Variables
#
# Copy this file to .env and fill in your API keys
# cp .env.example .env

# Anthropic Claude API (required for LLM integration tests)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# OpenAI API (optional - for multi-LLM support)
# Get your key from: https://platform.openai.com/api-keys
#OPENAI_API_KEY=your-openai-api-key-here

# Long-Term Memory Configuration (optional)
#PATTERN_STORAGE_DIR=.patterns/
#EMBEDDING_MODEL=text-embedding-ada-002

# JWT Authentication (required for backend API)
# Generate a secure key with: python -c 'import secrets; print(secrets.token_urlsafe(32))'
# Minimum 32 characters required for HS256 security
#JWT_SECRET_KEY=your-secure-random-key-minimum-32-chars

# Demo/Testing Keys (for examples/security_demo.py ONLY - not real keys)
#DEMO_STRIPE_API_KEY=sk_live_DEMO_NOT_REAL_FOR_TESTING
#DEMO_STRIPE_SECRET=whsec_DEMO_NOT_REAL_FOR_TESTING

# Redis Configuration (Optional - Enhanced Features)
# Enable Redis for session management, caching, and real-time agent coordination
REDIS_ENABLED=true

# REDIS_MODE: "cloud" (recommended) or "local"
#   cloud  - Uses REDIS_HOST, REDIS_PORT, REDIS_PASSWORD (for Redis Cloud, AWS ElastiCache, etc.)
#   local  - Uses localhost:6379, no password (for local Redis OSS / Docker)
#   If not set, mode is inferred from REDIS_HOST (non-localhost = cloud, localhost = local)
REDIS_MODE=cloud

# Redis Cloud / Managed Redis (when REDIS_MODE=cloud)
# Get a free Redis Cloud account at: https://redis.io/try-free/
REDIS_HOST=your-redis-cloud-host.redislabs.com
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=your-redis-cloud-password

# For local Redis OSS (uncomment and set REDIS_MODE=local):
# REDIS_MODE=local
# No REDIS_HOST or REDIS_PASSWORD needed - uses localhost:6379 automatically
# Install: brew install redis && brew services start redis
